<!DOCTYPE html>
<html lang="en">
  <%- include('partials/header') %>
  <body>
    <div class="container-fluid mx-auto" style="width: 350px">
      <h4>Sign Up</h4>
      <% if(message){ %> <span class="text-danger"><%= message %></span> <% } %>
      <form action="/signup" method="post">
        <div style="font-size: small">
          Already have an account?
          <a href="/signin" class="text-primary text-opacity-75">Sign in</a>
        </div>
        <div>
          <label for="email">Email</label><br />
          <input type="email" id="email" name="email" required />
        </div>
        <div>
          <label for="password">Password</label><br />
          <input type="password" name="password" id="password" required />
        </div>
        <div>
          <label for="secret">Secret</label><br />
          <input type="text" id="secret" name="secret" required />
        </div>
        <button type="submit" class="btn btn-primary">Sign Up</button>
        <p style="font-size: small">
          By clicking the "Sign Up" button you are creating an new account, and
          you agree with the Terms of Use
        </p>
      </form>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
